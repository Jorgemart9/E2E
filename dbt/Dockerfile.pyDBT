# E2E/dbt/Dockerfile.pyDBT
FROM python:3.12-slim

WORKDIR /usr/app/dbt

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt dbt-core dbt-postgres

COPY . .

CMD ["dbt", "build", "--target", "dev"]
